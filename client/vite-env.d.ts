/// <reference types="vite/client" />

// Brings in Vite's ambient module declarations for static assets
// (`*.webp`, `*.png`, …) so ESM asset imports type-check under
// tsconfig.client.json. Vite resolves each import to a served URL string.
