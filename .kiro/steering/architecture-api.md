---
inclusion: fileMatch
fileMatchPattern: "server/**"
---

# Architecture — Data Flow & API detail

Loaded because you're editing `server/**`. Covers client↔server data flow and
every API endpoint (`/api/generate`, `/api/world-tiles`, `/api/combat`,
`/api/ai-turn`, `/api/building-turn`, `/api/match/create`, `/api/match/intent`
including logistics intents), plus which paths are authoritative vs. legacy.

#[[file:../../docs/architecture/data-flow-and-api.md]]
